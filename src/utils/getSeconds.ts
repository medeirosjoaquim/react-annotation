// export const getSeconds = (duration: number) => {
//   const [hours, minutes, seconds] = parseInt(duration.split(":"))
//   const totalSeconds = +hours * 60 * 60 + +minutes * 60 + +seconds
//   return totalSeconds
// }
